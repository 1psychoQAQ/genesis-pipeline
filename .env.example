# Genesis Pipeline Configuration

# ===================
# Database (PostgreSQL)
# ===================
DB_HOST=localhost
DB_PORT=5433
DB_USER=genesis
DB_PASSWORD=genesis123
DB_NAME=genesis

# ===================
# Gemini AI
# ===================
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-api-key-here
# Available models: gemini-2.0-flash, gemini-1.5-pro, gemini-1.5-flash
GEMINI_MODEL=gemini-2.0-flash

# ===================
# Pipeline Defaults
# ===================
# Default search query (used when no -query or -question is provided)
DEFAULT_QUERY=machine learning
# Default number of papers to fetch
DEFAULT_LIMIT=10
# Minimum quality score (0-100)
DEFAULT_MIN_SCORE=60
# Maximum paper age in days (0 = no limit)
DEFAULT_MAX_AGE=365
